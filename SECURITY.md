# Security

Do not commit Telegram bot tokens, device API tokens, rclone credentials, vault files, or server environment files.

The example configuration contains placeholders only. If a bot token is ever disclosed, revoke it with BotFather and update the server environment before restarting the service.

